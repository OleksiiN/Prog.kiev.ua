	public static void printEvenRange(int first, int last) {
		if (last >= first) {
			if (first % 2 != 0) first++;
			for (int i = first; i <= last; i+=2) {
			System.out.print(i + " ");
			}
		}
	System.out.println();
	}
